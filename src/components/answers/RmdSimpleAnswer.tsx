import React from 'react';

const RmdSimpleAnswer = () => (
  <div>
    <div className="text-sm font-semibold text-zinc-700 mb-1">RMD: Required Minimum Distribution</div>
    <p className="text-base text-zinc-800 font-medium">
      A required minimum distribution (RMD) is the minimum amount you must withdraw each year from certain retirement accounts, starting at age 73. RMD rules help ensure you pay taxes on your tax-deferred savings.
    </p>
  </div>
);

export default RmdSimpleAnswer; 